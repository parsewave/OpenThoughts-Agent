#!/bin/bash
# Local development defaults for the dip.tu-dresden.de environment.
# Adjust paths as needed; variables are intentionally minimal.

export SCRATCH="$HOME"
export DCFT="$HOME/Documents/OpenThoughts-Agent"
export DC_AGENT="$DCFT"
export HF_CACHE_DIR="$SCRATCH/.cache/huggingface"
export HF_HUB_CACHE="$HF_CACHE_DIR/hub"
export DATASETS_DIR="$HF_HUB_CACHE"
export MODELS_DIR="$HF_HUB_CACHE"
export TOKENIZED_DATASETS_DIR="$SCRATCH/tokenized_datasets"
export WANDB_PROJECT="${WANDB_PROJECT:-OpenThoughts-Agent}"
export WANDB_ENTITY="${WANDB_ENTITY:-local}"
# SkyRL home directory (clone from https://github.com/penfever/SkyRL.git)
export SKYRL_HOME="$HOME/SkyRL"
# Keep the repo importable even if Python strips the working directory from sys.path.
export PYTHONPATH="${DCFT_PRIVATE:-$DCFT}${PYTHONPATH:+:$PYTHONPATH}"
